import 'package:flutter/material.dart';

// TODO Phase 2.5: Implement ModelDownloadScreen
// - Progress bar for model loading / download
// - Supports bundled-asset scenario (current) and future download
// - Romantic loading animation
// - Navigate to ChatScreen when model is ready

class ModelDownloadScreen extends StatelessWidget {
  const ModelDownloadScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Model Download — Phase 2.5')));
  }
}
