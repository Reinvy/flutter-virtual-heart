import 'package:flutter/material.dart';

// TODO Phase 4.1: Implement SettingsScreen sections:
// - Persona (name, personality, avatar, nickname, reset)
// - Theme (dark / light / system)
// - AI Language (Indonesian / English / Mixed)
// - Voice (TTS toggle, auto-play, voice picker)
// - Notifications (morning message, check-in)
// - Data & Privacy (local storage label, delete all, export)

class SettingsScreen extends StatelessWidget {
  const SettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Settings — Phase 4.1')));
  }
}
