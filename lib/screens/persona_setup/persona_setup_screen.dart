import 'package:flutter/material.dart';

// TODO Phase 2.4: Implement PersonaSetupScreen
// - Gender picker (girlfriend / boyfriend)
// - Name input (required)
// - Personality preset (Gentle / Cheerful / Mature / Mysterious)
// - Hobbies multi-select tags
// - Nickname for user (required)
// - Avatar gallery (4–8 per gender)
// - Save PersonaConfig via ObjectBox

class PersonaSetupScreen extends StatelessWidget {
  const PersonaSetupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Persona Setup — Phase 2.4')));
  }
}
