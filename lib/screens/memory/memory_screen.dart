import 'package:flutter/material.dart';

// TODO Phase 3.1: Implement MemoryScreen
// - List of memory cards grouped by category
// - Swipe-to-delete per fact
// - "Reset All Memory" button with confirmation dialog

class MemoryScreen extends StatelessWidget {
  const MemoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Memory — Phase 3.1')));
  }
}
