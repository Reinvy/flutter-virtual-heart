# Taste
- Prefers all project documentation and planning documents to be written in Bahasa Indonesia (Indonesian), even when working on apps whose code/content is in English. Confidence: 0.9
- Prefers feature-first modular architecture: code organized by feature (e.g. `lib/features/<feature>/`) with shared core/data/services layers, rather than pure layered (core/data/screens/services) structure. Confidence: 0.9
- When a project is slated for a full refactor, prefers documentation (agents.md, rules, workflow, design system, feature specs) to be written fresh as a target-state contract — describing the desired new architecture and features, not the current implementation — so the codebase is rebuilt to match the docs. Confidence: 0.9
- Prefers the app's default theme to be light mode (`ThemeMode.light`), with dark/system as user-selectable options rather than the default. Confidence: 0.9
- Prefers a romantic, warm, intimate visual aesthetic for the app: blush-toned background, rose pink/mauve accents, soft surfaces, and romantic gradients — i.e., light mode designed around a romantic sakura-pink palette rather than generic white. Explicitly requested a "romantic theme with sakura pink" inspired by Yae Miko / Genshin Impact's Inazuma aesthetic. Confidence: 0.9
- When undertaking a refactor, prefers a full, complete refactor of the entire codebase to match the target docs, rather than partial or incremental changes. Confidence: 0.8
- When upgrading dependencies, prefers a full-latest migration to the newest major versions of every package (e.g. flutter_gemma 0.12→1.x, Riverpod 2→3), and accepts doing the substantial code rewrites that breaking changes require — over staying on older/pinned major lines. Confidence: 0.85
- Prefers fully pinning the Flutter/Dart version across all version config at once (FVM `.fvmrc`, `.vscode/settings.json` flutterSdkPath, and the pubspec `environment.sdk` constraint) so every tool uses one consistent version, rather than pinning it in only one place. Confidence: 0.8
- Values verified, compilable outcomes as the acceptance bar for migration work: keeps Flutter 3.47.x local to run `flutter analyze` (0 issues) and `flutter test` (all passing) as evidence a migration is complete, rather than delivering code that is merely "assumed correct.". Confidence: 0.7
- When undertaking a refactor, prefers also resolving Android build/toolchain warnings by upgrading to the latest supported versions (Gradle, Android Gradle Plugin, Kotlin, and `compileSdk` to the highest SDK required by plugins) rather than bypassing validation or leaving the toolchain outdated. Confidence: 0.6
d-design) and design-system references to avoid clichéd AI patterns (generic purple–teal gradients, empty centered heroes, default Material purple bleeding into controls). Confidence: 0.9
- Prefers grounding UI design work in researched, real-world references: explicitly asks to use the internet to source design assets and references (icons, design systems, color palettes, typography, fonts) rather than relying on defaults or inventing them. Confidence: 0.9
- Centralizes all user-facing strings in a single l10n layer (`AppStrings`) with both Indonesian and English implementations; no hardcoded UI strings in widgets — migrates leftover hardcoded strings (e.g. tooltips) into the l10n layer during cleanup. Confidence: 0.85
- Follows the documented git workflow for delivering changes: dedicated `feat/` branch off `development`, atomic commits in Conventional Commits format, and keeps commits local — push/PR to origin only when the user explicitly asks. Confidence: 0.7
- Prefers project docs (`docs/` — DESIGN, FEATURES, WORKFLOW, AGENTS) to be tracked in version control and kept in sync with the code, rather than gitignored or drifting stale. Confidence: 0.7
